# SonarCloud automatic analysis configuration.
#
# HelpPage.tsx is documentation content: it embeds many long worked example
# listings as template literals. The copy-paste detector anonymizes string
# literals, so distinct examples register as huge false-positive duplicate
# blocks. Exclude the file from duplication detection only (all other rules
# still apply to it).
sonar.cpd.exclusions=frontend/src/HelpPage.tsx
